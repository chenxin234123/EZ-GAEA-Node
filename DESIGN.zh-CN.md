# EZ Gaea Node 的制作思路

[English](DESIGN.md) · [项目介绍](README.zh-CN.md)

## 为什么制作它

Gaea 的节点名称和参考说明主要使用英文。对英语不是第一语言的使用者而言，制作地形时反复查词、切换网页会打断思路。我希望把节点说明放在 Gaea 旁边，让大家用自己熟悉的语言理解工具，同时保留英文名称方便对照。

## 核心思路

它将“本地文档库”和“当前节点识别”连接起来：

**搜索节点，或在 Gaea 中点击 → 确认节点类型 → 找到本地文档 → 按所选语言显示。**

日常使用时，它是在查找已经整理好的说明，不会每次点击都向 AI 提问。译文提前准备并保存在本地，因此查阅不需要联网翻译，也不需要用户提供 AI 账号或 API 密钥。

## 如何准备文档

从[官方节点参考目录](https://docs.gaea.app/reference)收集节点页面，将内容整理为节点介绍、参数分组、参数说明、选项、使用场景和视觉参考。另加入[官方 Macro 文档](https://docs.gaea.app/developers/extensibility/macros/)。每个节点保留原始英文标识和来源链接，方便对照和后续维护。

如果原文只给出了参数名称、没有解释，就保留名称，不凭空补写说明。当前内置库是包含 183 个节点的文档快照；官网以后新增或修改内容，需要再整理更新，并不是实时同步整个官网。

## 如何知道你点了哪个节点

助手在 Windows 中监听鼠标松开与前台窗口变化，先判断目标是不是符合条件的 Gaea 程序，再通过 Windows UI Automation 读取属性面板公开的文字信息，包括节点类型标题、参数分组、参数名和帮助文字。这条识别流程不依赖截图 OCR。

随后把这些线索与本地节点目录比较，确定应该显示哪一份文档。已知的名称差异使用明确的别名映射，例如 Gaea 中的 FlowClassic 可以对应文档中的 FlowMapClassic，文档原名仍然保留。对于少参数或无参数的特定节点，再结合选中数量和稳定的节点类型标题进行确认。

信息不足或存在多个候选时，助手会保留当前说明并提示用户选择候选或手动搜索。整体设计以一次查看一个节点为主。

## 如何减少等待和卡顿

连续点击会短暂合并，属性读取放在独立的辅助进程中，避免直接阻塞主窗口。缓存减少重复读取；超时机制处理没有及时回应的请求；新旧请求检查防止旧结果覆盖新选择。连续超时会暂停跟随，手动搜索仍然可用。这些措施用于降低风险，并不意味着所有 Gaea 版本的界面信息都完全相同。

## 如何支持多语言

用于识别的节点标识和显示给用户的译文分开保存。节点名和参数名保留英文，旁边可以显示译名。正文按照勾选的语言逐段显示，也可以用翻译后的节点名搜索。

英文来自现有参考文档；简体中文沿用已有第三方译文，繁体中文由简体转换。日本語、Deutsch、Español、Français、한국어 由 GPT-6 重新翻译，每种包含 2,926 条独立的文档及界面文字，并进行了覆盖、术语、数字、条件检查和语境抽查。这不等同于专业母语译者逐条审校。

## 软件与 Gaea 的关系

这是使用 C#、WPF 和 .NET Framework 4.8 制作的独立 Windows 助手。它不需要把组件安装到 Gaea 内部，也不负责生成地形。正常跟随流程读取界面公开的信息，不修改节点参数。使用设置和最近一次识别的诊断信息保存在本地 UserData 中；点击官网或作者链接时才打开浏览器。

当前自动跟随检查的是 Gaea.exe，且安装路径包含 \Gaea 2\。不能据此保证后续所有版本都兼容；不运行 Gaea 时也可以使用手动搜索查阅文档。

## 我公开的内容

这里分享编译好的软件、使用说明和制作思路，不公开应用程序的实现源码和构建工程。下载包中的 JSON 是运行必需的文档与语言数据。Gaea2-Docs 文档保留原有 MIT 声明，该声明不代表整个助手程序采用 MIT 许可证。

作者：[chenxin234123](https://www.artstation.com/chenxin234123)。

## 手动更新入口

三点菜单中的“检查更新”只在默认浏览器打开 https://github.com/chenxin234123/EZ-GAEA-Node，菜单会随界面语言切换。程序没有版本检查、更新包下载或自动安装服务。
